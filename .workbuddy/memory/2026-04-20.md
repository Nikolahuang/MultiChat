# 2026-04-20

## macOS 打包配置
- 更新 tauri.conf.json：bundle.targets 从 ["nsis"] 改为 ["nsis", "dmg"]，新增 bundle.macOS 配置节（minimumSystemVersion: "10.15", entitlements: "MultiChat.entitlements"）
- 创建 src-tauri/MultiChat.entitlements：声明网络、文件、JS执行、内存权限
- 创建 .github/workflows/build-macos.yml：GitHub Actions CI 工作流，支持 arm64 + x64 双架构构建
- 创建 docs/macos-build-guide.md：完整打包指南
- Rust 代码验证通过：无平台特定条件编译，PhysicalPosition/PhysicalSize 跨平台一致
- icon.icns (88KB) 已存在，无需额外准备
- 修复 bundle identifier：com.multichat.app → com.multichat.desktop（避免 macOS .app 扩展名冲突）
- Windows 构建验证通过（NSIS 安装包正常生成）
- 代码推送到 GitHub: git@github.com:Nikolahuang/MultiChat.git
- v0.1.0 tag 已推送，GitHub Actions macOS CI 已触发（arm64 + x64 双架构）
- CI Run: https://github.com/Nikolahuang/MultiChat/actions/runs/24670523130
- gh CLI 路径: "C:\Program Files\GitHub CLI\gh.exe"（不在 PATH 中）
- git 全局 URL 重写 ghproxy 已临时取消（连接失败）
